{
  "nav.skip": "Skip to content",
  "nav.architecture": "Architecture",
  "nav.installation": "Installation",
  "nav.mcp": "MCP",
  "nav.start": "Get started",
  "lang.aria": "Language",
  "brand.tagline": "A collaborative operating system for people and AI agents.",
  "foot.home": "Home",
  "foot.arch": "Architecture",
  "foot.install": "Installation",
  "foot.mcp": "MCP server",
  "foot.checklist": "Tool checklist",
  "foot.repo": "Repository",
  "foot.releases": "Releases",
  "foot.license": "License",
  "foot.copyright": "Copyright 2026 Federico Pereira. Co-authored by Nicolas Pereira. Licensed under the Apache License, Version 2.0.",
  "foot.trademark": "\"SentinelDesk\" and its logo are trademarks of Federico Pereira and are not covered by the code license. You may fork, modify and redistribute the code freely; you may not call the result SentinelDesk or use its branding without permission.",
  "home.title": "SentinelDesk — A collaborative operating system for people and AI agents",
  "home.eyebrow": "Apache 2.0 · Debian 13 · amd64 and arm64",
  "home.h1": "A collaborative operating system for people and <span class='accent'>AI agents</span>",
  "home.lede": "A complete Linux desktop running inside a container with no physical monitor, streamed to the browser over <strong>WebRTC</strong> — and driven, at the same time, by people and by an agent that sees and acts on the same screen. Same X display, same session, same room.",
  "home.cta.start": "Get started",
  "home.cta.docs": "Read the docs",
  "home.note": "Published here, in three languages. The desktop's <strong>Docs</strong> button opens this same guide.",
  "home.demo.you": "You",
  "home.demo.agent": "AI agent",
  "home.demo.chip.you": "You are driving",
  "home.demo.chip.ask": "AI agent is asking for control",
  "home.demo.chip.agent": "AI agent is driving",
  "home.demo.readout": "<em>114</em> tools · <em>50–70 ms</em> glass-to-glass · <em>1</em> encode → <em>4</em> viewers",
  "home.demo.cap": "A loop of the turn-taking, not a live session. While a person is connected the agent has to ask, and the answer is theirs to give; with an empty room it simply works, because there is nobody to take turns with.",
  "home.planes.kicker": "The idea",
  "home.planes.h2": "Two independent control planes over one desktop",
  "home.planes.p": "The second half is not a metaphor for an API. A person drives through a control layer in the browser; an agent drives through a local Unix socket. Neither is a guest of the other, and that separation is the single most important thing about the design.",
  "home.c1.h3": "People, over WebSocket",
  "home.c1.p": "<code>/ws</code> is the only door. The first frame must be an authentication frame; until it validates there is no SDP offer, no ICE and no DataChannel. Keyboard and mouse arrive on a data channel and go into X through XTEST.",
  "home.c2.h3": "Agents, over a local socket",
  "home.c2.p": "A <code>0600</code> Unix socket exposes 114 tools to any MCP-capable host. The bridge is a thin stdio pipe spawned with <code>docker exec</code>, so killing the host never takes the desktop down with it.",
  "home.arch.kicker": "Architecture",
  "home.arch.h2": "One capture, many observers",
  "home.arch.p1": "The screen is encoded <strong>once</strong> and fanned out: to every browser, to the recorder, to a live stream. A second participant costs bandwidth, not CPU. GStreamer runs inside the Go process, so every RTP packet goes from <code>appsink</code> straight onto the WebRTC track and the encoder stays reachable at runtime.",
  "home.arch.p2": "The encoder is picked at startup by running a real probe pipeline for each candidate — NVENC, VA-API, x264, then software VP8 — so the choice reflects what actually works on the host rather than what the drivers claim.",
  "home.arch.link": "See the full architecture →",
  "home.arch.alt": "SentinelDesk architecture: the container's X display captured once by GStreamer, encoded and fanned out over WebRTC to several browsers, with a parallel MCP control plane over a local Unix socket.",
  "home.arch.cap": "Capture, encode and fan-out, with the two control planes either side.",
  "home.cap.kicker": "What it does",
  "home.cap.h2": "A real desktop, not a screenshot service",
  "home.f1.h3": "They take turns",
  "home.f1.p": "Exactly one participant holds control. While a person is connected the agent has to ask, and a prompt appears on that person's screen with a timer; no answer means no.",
  "home.f2.h3": "Adaptive by design",
  "home.f2.p": "Congestion estimation over TWCC steers the bitrate, a lost reference frame is answered with an immediate keyframe, and the shared encoder follows the slowest link so the worst connection is never dropped.",
  "home.f3.h3": "Structure, not pixels",
  "home.f3.p": "An agent reads the screen as an accessibility tree or drives the real DOM through DevTools, so it clicks a button by name instead of guessing at coordinates.",
  "home.f4.h3": "Audio both ways",
  "home.f4.p": "The desktop's sound reaches the browser, and the browser's microphone comes back as an ordinary capture device inside the desktop — a real input, not a monitor a page will refuse to list.",
  "home.f5.h3": "Files, clipboard, gamepad",
  "home.f5.p": "A two-pane manager with Midnight Commander keys, drag-and-drop onto the page, a two-way clipboard, and a browser gamepad that appears inside as a virtual Xbox 360 pad.",
  "home.f6.h3": "Record and stream out",
  "home.f6.p": "Record to MP4, WebM or MKV, or send the live session to YouTube, Twitch or your own VLC — reusing the encode the room is already producing rather than capturing twice.",
  "home.quick.kicker": "Quick start",
  "home.quick.h2": "One command on a machine of its own",
  "home.quick.p": "The installer downloads one binary and asks <em>it</em> for everything else: the binary embeds its own deployment files, so configuration and code can never disagree about versions. <code>auto</code> picks Docker when it is present and a native systemd install otherwise.",
  "home.quick.btn": "Full installation guide",
  "home.quick.pre": "<span class='c'># VPS or Raspberry Pi 5 (Debian 13 / Raspberry Pi OS)</span>\ncurl -fsSL https://raw.githubusercontent.com/\\\nlordbasex/sentineldesk/main/install.sh | sudo bash -s auto\n\n<span class='c'># or, from a clone, for development</span>\nmake up   <span class='c'># → http://localhost:8080</span>",
  "home.quick.note": "Set <code>AUTH_USER</code> and <code>AUTH_PASS</code> before exposing it. Without them authentication is disabled, which is for LAN development only.",
  "home.final.h2": "Put a person and an agent on the same screen",
  "home.final.p": "Everything is in one repository, under Apache 2.0: the daemon, the browser client, the deployment tree and the installer.",
  "home.final.cta1": "Get started",
  "home.final.cta2": "Read the docs",
  "arch.title": "Architecture — SentinelDesk",
  "arch.eyebrow": "Architecture",
  "arch.h1": "One desktop, two control planes",
  "arch.lede": "Everything else follows from that separation: people come in over a WebSocket, the agent over a local Unix socket, and neither is a guest of the other.",
  "arch.alt": "SentinelDesk architecture diagram: the container's X display captured once by GStreamer, encoded and fanned out over WebRTC to several browsers, with the MCP control plane in parallel over a local Unix socket.",
  "arch.cap": "Capture, encode and fan-out, with the two control planes either side. <em>Note: the diagram is labelled 106 tools; the current catalogue has 114.</em>",
  "arch.desktop.h2": "The desktop",
  "arch.desktop.p1": "<code>Xvfb :0</code> provides a virtual display with no physical monitor behind it. <strong>Openbox</strong> manages window frames and nothing else — it does not draw the desktop. That job belongs to <code>pcmanfm</code> in desktop mode, which owns the root window and paints the wallpaper and icons, with <code>lxpanel</code> on top.",
  "arch.desktop.p2": "Everything runs under <strong>supervisord</strong>, so any piece restarts on its own without taking the rest down: <code>xvfb</code>, <code>pulseaudio</code>, <code>dbus-session</code>, <code>at-spi</code>, <code>openbox</code>, <code>pcmanfm-desktop</code>, <code>lxpanel</code> and <code>sentineldesk</code> itself.",
  "arch.video.h2": "The media path, outbound",
  "arch.video.p1": "<code>ximagesrc</code> reads the framebuffer and hands it to GStreamer, which runs <em>inside</em> the Go process through go-gst rather than as a separate <code>gst-launch</code> child. That is what keeps the encoder reachable while it runs: bitrate changes, keyframes on demand, and destinations attached and detached without cutting anything.",
  "arch.video.p2": "The encoder is chosen at startup by running a real probe pipeline for each candidate, in order. The choice reflects what actually works on that host, not what the drivers claim to support.",
  "arch.th.order": "Order",
  "arch.th.enc": "Encoder",
  "arch.th.hw": "Hardware",
  "arch.th.codec": "Codec",
  "arch.video.p3": "Encoded frames leave through an <code>appsink</code> and each RTP packet goes straight onto the Pion track. On the H.264 encoders they also pass through a <code>tee</code>: in the normal case nothing is attached to it, and that is where an external destination gets a copy of the same picture the room is already receiving. Streaming to YouTube does not mean encoding the screen a second time.",
  "arch.tee.note": "<b>Why the <code>tee</code> sits before <code>h264parse</code>.</b> Each branch parses for itself: WebRTC wants byte-stream and FLV wants AVC. Asking one parser to produce both deadlocks the negotiation. Parsing is nearly free — it reads headers, it does not decode — so the duplication costs nothing next to a second encoder.",
  "arch.audio.h3": "Audio",
  "arch.audio.p": "There is no sound card, so PulseAudio loads a <em>null sink</em> named <code>sentineldesk</code>. Applications play into it and <code>pulsesrc sentineldesk.monitor</code> captures what they played; <code>opusenc</code> encodes it and a second <code>appsink</code> feeds the audio track.",
  "arch.mic.h2": "The way back: the microphone",
  "arch.mic.p1": "The browser's microphone travels the other way, and the return path needs two PulseAudio objects rather than one:",
  "arch.mic.pre": "incoming Opus track → appsrc → pulsesink device=sentineldesk_mic\n                             → null sink \"sentineldesk_mic\"\n                               → sentineldesk_mic.monitor\n                                 → module-remap-source \"sentineldesk_mic_in\"",
  "arch.mic.p2": "The <em>remap</em> is not decoration. A monitor is how PulseAudio exposes \"what a sink is playing\", and applications treat it as such: browsers list monitors separately from real inputs, or hide them from the microphone picker altogether. Remapped, the same audio presents as an ordinary capture device — and it is made the system default.",
  "arch.mic.p3": "Both objects are created <strong>at startup</strong>, not on first use. A page enumerates its audio devices when it loads, so a microphone that appears later is missing from the list of the very page that wanted it.",
  "arch.human.h2": "Control plane 1 — people, over WebSocket",
  "arch.human.p1": "<code>WS /ws</code> is <strong>the only door</strong>. The first frame must be <code>{type:\"auth\"}</code>; until it validates there is no SDP offer, no ICE, no DataChannel. After authentication the same socket carries signalling and presence.",
  "arch.human.p2": "<code>HTTP :8080</code> serves the client embedded with <code>go:embed</code> and the file-manager endpoints, and holds <strong>no secrets</strong>: the ICE configuration and TURN credentials travel over the already-authenticated WebSocket. The only informational endpoint is <code>/auth</code>, which says nothing beyond whether a login is required. <strong>There is no <code>/login</code> endpoint, by design</strong>: HTTP is not the authentication gate.",
  "arch.human.p3": "Keyboard and mouse arrive over a DataChannel named <code>input</code> and are injected into X through XTEST. A browser gamepad becomes a virtual Xbox 360 pad through uinput, where the host exposes <code>/dev/uinput</code>.",
  "arch.agent.h2": "Control plane 2 — the agent, over a local Unix socket",
  "arch.agent.p1": "The MCP server listens on <code>/run/user/1000/sentineldesk-mcp.sock</code>, mode <code>0600</code>, and exposes <strong>114 tools</strong>. The AI host spawns <code>sentineldesk -mcp-stdio</code> with <code>docker exec</code>; that sub-command is a thin JSON-RPC pipe between stdin/stdout and the socket. Killing the host never takes the desktop down with it.",
  "arch.agent.p2": "Two helper bridges cover what pixels cannot: <code>a11y.py</code> exposes the AT-SPI accessibility tree (the <code>ui_*</code> tools, which invoke a button by name rather than by coordinates) and Chromium runs with CDP on port 9222 (the <code>browser_*</code> tools, which drive the real DOM).",
  "arch.agent.p3": "What the agent may do is bounded by a three-level policy — <code>full</code>, <code>safe</code>, <code>readonly</code> — plus a denylist and an allowlist. The daemon sets the ceiling through the environment, and each connection can restrict itself further but <strong>never widen</strong>.",
  "arch.th.level": "Level",
  "arch.th.allows": "What it lets through",
  "arch.pol.full": "Everything (the default)",
  "arch.pol.safe": "Everything except running code or touching the system; <code>as_root</code> is out",
  "arch.pol.readonly": "Observation only: see the screen, read the tree, list things",
  "arch.room.h2": "The room, and one deliberate asymmetry",
  "arch.room.p1": "Capture happens <strong>once</strong> and is fanned out to up to four participants (<code>MAX_VIEWERS</code>), so a second viewer does not cost a second encoder. One person drives at a time; the rest watch, and control is handed over cooperatively: everyone arrived with the same credential, so there is no hierarchy to enforce.",
  "arch.room.p2": "The shared encoder's bitrate is the <strong>minimum</strong> of what each network estimates, because encoding for the best link would drop the worst one.",
  "arch.room.p3": "Each participant's pointer is drawn as a <strong>real X window</strong>, not as an overlay in each browser. That distinction matters: because the pointer is part of the desktop, it appears in recordings, in screenshots and in every other viewer's stream, instead of existing only in one browser's DOM.",
  "arch.asym": "<b>The asymmetry, stated plainly:</b> MCP tools do <strong>not</strong> go through the room's control arbitration. They inject straight into XTEST, because the MCP arrives over the daemon's local socket rather than over the web. Arbitration exists between <em>browsers</em>. To stop an agent from touching anything, the instrument is its policy (<code>-mcp-policy readonly</code>), not room control.",
  "arch.net.h2": "Networking",
  "arch.net.p1": "On Linux with <code>network_mode: host</code>, ICE connects directly over UDP with nothing in between — the fastest path. On macOS and Windows the Docker Desktop VM breaks direct ICE, so <code>deploy/docker-compose.yml</code> includes <strong>coturn</strong> as a relay. For clients behind strict corporate NAT, publish coturn in production too and pass its URLs through <code>CLIENT_TURN_URLS</code>.",
  "arch.net.p2": "If the server sits behind NAT and does not hold the public IP on its own interface, set <code>NAT1TO1_IP=&lt;public IP&gt;</code> so ICE advertises the right address.",
  "arch.code.h2": "How the code is organised",
  "arch.code.pre": "cmd/sentineldesk/   wiring only: flags, HTTP, WebSocket, MCP socket\ninternal/config/    environment configuration\ninternal/desktop/   X11: input injection, cursor, clipboard, joystick, pointers\ninternal/media/     GStreamer: pipelines, encoders, recording, upstream audio\ninternal/stream/    sessions, the shared room, auth, rate limiting, TLS, files\ninternal/mcp/       the MCP server and its 114 tools\ninternal/webui/     the browser client, embedded with go:embed\ndeploy/             Dockerfile, compose, desktop and supervisor config",
  "arch.code.p": "The backend is <strong>a single Go binary</strong> (Pion WebRTC + go-gst). Because it uses CGO to link GStreamer, it cannot be cross-compiled with a bare <code>GOOS</code>/<code>GOARCH</code>: release binaries are built inside the Debian 13 Docker stage and extracted, so they are byte-for-byte what the container runs.",
  "arch.next": "Continue with the installation →",
  "install.title": "Installation — SentinelDesk",
  "install.eyebrow": "Installation",
  "install.h1": "A binary that brings its own deployment",
  "install.lede": "The installer downloads one binary and asks <em>it</em> for everything else. The binary embeds its own deployment files, so configuration and code can never disagree about versions: there is no second download to fall out of step with.",
  "install.own.h2": "On a machine of its own",
  "install.own.p1": "For a VPS or a Raspberry Pi 5 (Debian 13 / Raspberry Pi OS, amd64 or arm64):",
  "install.own.p2": "<code>auto</code> picks Docker when it is present and a native systemd install otherwise; <code>docker</code> or <code>native</code> choose explicitly. A running binary can also hand its files to another machine: <code>sentineldesk -install</code> serves them over HTTP and <code>sentineldesk -extract-deploy &lt;dir&gt;</code> writes them to disk.",
  "install.own.note": "Every build knows what it is: <code>sentineldesk -version</code> prints something like <code>sentineldesk v1.2.3 (081b14d) · build 20260804-190000</code>, and the same string appears at the bottom of the control rail.",
  "install.dev.h2": "For development, from a clone",
  "install.dev.pre": "make up     <span class='c'># build the image and start the desktop</span>\nmake logs   <span class='c'># follow the logs</span>\nmake shell  <span class='c'># a root shell inside the container</span>\nmake down   <span class='c'># stop everything</span>",
  "install.dev.p": "Then <a href='http://localhost:8080'>http://localhost:8080</a>. With <code>AUTH_USER</code> and <code>AUTH_PASS</code> set it asks for a login; without them it is open, and that is <strong>for development only</strong>.",
  "install.prod.h2": "Production",
  "install.prod.p": "Pick the compose file that matches the server's hardware. All three use <code>network_mode: host</code> for a direct ICE connection, which is the lowest latency.",
  "install.prod.pre": "<span class='c'># CPU only (software VP8)</span>\nAUTH_USER=admin AUTH_PASS=secret \\\ndocker compose -f deploy/docker-compose.cpu.yml up -d\n\n<span class='c'># NVIDIA GPU (NVENC) — needs nvidia-container-toolkit</span>\nAUTH_USER=admin AUTH_PASS=secret \\\ndocker compose -f deploy/docker-compose.nvidia.yml up -d\n\n<span class='c'># Intel/AMD GPU (VA-API) — passes /dev/dri through</span>\nAUTH_USER=admin AUTH_PASS=secret \\\ndocker compose -f deploy/docker-compose.vaapi.yml up -d",
  "install.prod.warn": "<b>Before exposing it to the internet</b>, always set <code>AUTH_USER</code> and <code>AUTH_PASS</code>: the production compose files require them. Without them authentication is disabled, which is good for a development LAN and for nothing else.",
  "install.tls.h2": "TLS and certificates",
  "install.tls.p": "Two ways to serve HTTPS, depending on whether you have a proxy in front.",
  "install.tls.a.h3": "A. Native TLS in the backend",
  "install.tls.a.p": "No Caddy, a single port.",
  "install.th.vars": "Variables",
  "install.th.result": "Result",
  "install.tls.self": "Generates a <strong>self-signed certificate</strong> once and persists it in the volume. SANs: localhost, the hostname, plus whatever you add in <code>TLS_HOSTS=ip,domain</code>. Good for a LAN or for testing.",
  "install.tls.own": "Uses <strong>your own certificate</strong>: a purchased wildcard, a corporate one, or one issued by certbot. Copy the files into <code>deploy/certs/</code>, which is mounted as <code>/certs</code>.",
  "install.tls.pre": "TLS_SELFSIGNED=1 docker compose -f deploy/docker-compose.yml up -d\n<span class='c'># → https://localhost:8080</span>",
  "install.tls.b.h3": "B. Caddy as a reverse proxy",
  "install.tls.b.p": "With <code>--profile tls</code> on the production compose files, choosing the mode with <code>TLS_MODE=</code>.",
  "install.th.mode": "Mode",
  "install.th.cert": "Certificate",
  "install.th.needs": "Requires",
  "install.tls.auto.c": "<strong>Let's Encrypt</strong> automatically (HTTP-01), renewing itself",
  "install.tls.auto.n": "<code>DOMAIN</code> pointing at the server, ports 80/443",
  "install.tls.ss.c": "<strong>Self-signed</strong> by Caddy's internal CA",
  "install.tls.ss.n": "Nothing: works by IP or local name",
  "install.tls.cu.c": "<strong>Your certificate / wildcard</strong> from <code>deploy/certs/</code>",
  "install.tls.cu.n": "The files in <code>deploy/certs/</code>",
  "install.tls.wc.c": "<strong>Let's Encrypt wildcard</strong> <code>*.domain</code> (DNS-01 through Cloudflare)",
  "install.tls.wc.n": "An image with the plugin + <code>CLOUDFLARE_API_TOKEN</code>",
  "install.tls.pre2": "<span class='c'># Classic Let's Encrypt</span>\nDOMAIN=desktop.example.com AUTH_USER=admin AUTH_PASS=secret \\\ndocker compose -f deploy/docker-compose.nvidia.yml --profile tls up -d\n\n<span class='c'># Self-signed through Caddy (no domain)</span>\nTLS_MODE=selfsigned AUTH_USER=admin AUTH_PASS=secret \\\ndocker compose -f deploy/docker-compose.cpu.yml --profile tls up -d",
  "install.tls.p2": "In any of these modes the WebSocket switches to <code>wss://</code> on its own. Ports to open in the firewall: <strong>80/443</strong> for the web, and WebRTC's <strong>UDP</strong> range (or the fixed one you set with <code>WEBRTC_MIN_PORT</code> / <code>WEBRTC_MAX_PORT</code>).",
  "install.vars.h2": "Environment variables",
  "install.vars.p": "Configuration is environment-only: the container is the unit of deployment, and a separate configuration file would just be one more thing that can disagree with the compose file.",
  "install.th.var": "Variable",
  "install.th.def": "Default",
  "install.th.does": "What it does",
  "install.v.res": "Virtual desktop resolution",
  "install.v.fps": "Frames per second (60 is worth it with a GPU)",
  "install.v.enc": "<code>auto</code>, <code>nvenc</code>, <code>vaapi</code>, <code>h264</code>, <code>vp8</code>",
  "install.v.vbr": "Bitrate ceiling; the adaptive loop only goes lower",
  "install.v.abr": "Opus bitrate, in bps",
  "install.v.cur": "<code>1</code> draws the pointer on the server (more perceived latency)",
  "install.v.max": "How many people share one desktop",
  "install.v.auth": "Credentials (empty = no auth, development only)",
  "install.v.secret": "HMAC key for session tokens; pin it to survive restarts",
  "install.v.ttl": "Session lifetime",
  "install.v.files": "How far the file manager may reach",
  "install.v.nat": "Public IP to advertise in ICE (server behind NAT)",
  "install.v.ports": "Fixed UDP range for ICE",
  "install.v.turn": "TURN for the browser (NAT fallback)",
  "install.v.mcp": "The MCP permission ceiling",
  "install.v.port": "Port for the web client and signalling",
  "install.mcp.h2": "Connecting an agent over MCP",
  "install.mcp.p1": "The bridge runs <strong>inside</strong> the container, so the AI host launches it with <code>docker exec</code>. In the host's MCP configuration:",
  "install.mcp.li1": "<code>-i</code> keeps stdin open: that is the MCP transport.",
  "install.mcp.li2": "<code>-u sentineldesk</code> runs as the socket's owner (uid 1000).",
  "install.mcp.li3": "The container has to be named <code>sentineldesk</code>, or adjust the name.",
  "install.mcp.p2": "To hand an agent a read-only endpoint against the same daemon you use with full permissions, add <code>-mcp-policy readonly</code> to those arguments. A connection can drop below the ceiling the daemon set, never raise it.",
  "install.steps.h2": "Getting it running, end to end",
  "install.st1.h3": "Start the desktop",
  "install.st1.p": "Whichever compose file matches the hardware, with <code>AUTH_USER</code> and <code>AUTH_PASS</code> set.",
  "install.st2.h3": "Open it in the browser",
  "install.st2.p": "Port <code>8080</code> by default. Whoever arrives first takes control; the rest watch and can ask for it.",
  "install.st3.h3": "Add TLS if it faces the internet",
  "install.st3.p": "Native TLS with <code>TLS_SELFSIGNED=1</code>, or Caddy with <code>--profile tls</code> and a domain.",
  "install.st4.h3": "Plug the agent in",
  "install.st4.p": "The MCP bridge over <code>docker exec</code>, with whatever policy you want to give it.",
  "install.back": "← How it works inside",
  "install.final.h2": "Instance ready?",
  "install.final.p": "The full user guide, in three languages — the same one the desktop's rail links to.",
  "install.final.cta1": "Read the documentation",
  "install.final.cta2": "See the repository"
}
